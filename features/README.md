# Feature Catalog

기능 단위 번들은 `features/<feature-id>/` 아래에 위치합니다.

각 기능은 아래를 포함합니다.

- `feature.yaml`: 이름/버전/요약/컴포넌트 목록
- `README.md`: 기능 설명 및 사용법
- `components/`: 설치 가능한 실제 자산 (`agents`, `skills`, `mcp`)

현재 기능:

- `codex-collab-orchestrator`
- `playwright`
