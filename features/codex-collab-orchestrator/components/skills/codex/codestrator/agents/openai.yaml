interface:
  display_name: "Codestrator"
  short_description: "Root orchestrator for parallel Codex work with worktree isolation"
  default_prompt: "Coordinate parallel Codex sessions: branch into worktree, decompose tasks, spawn child workers with codestrator-worker skill, and finish with lock-guarded merge review."
  brand_color: "#4A90D9"

dependencies:
  tools:
    - codex_collab_orchestrator_codex_orchestrator

metadata:
  version: "0.2.0"
  category: "orchestration"
  requires_runtime:
    - tmux
    - git
