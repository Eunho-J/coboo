interface:
  display_name: "Codex Work Orchestrator"
  short_description: "Run parallel Codex work with lock and checkpoint controls"
  default_prompt: "Bootstrap orchestration via tmux root Codex delegation, then run strict case-level checkpoints with lock discipline."
